services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    ports:
      - "3100:3000"
    env_file: .env
    restart: unless-stopped

  bot:
    build:
      context: .
      dockerfile: Dockerfile.bot
    env_file: .env
    restart: unless-stopped
